```
const myFunc = ({title, url, description, rating}) => ({title, url, description, rating})
(myFunc)(this.state);

. . .

const myFunc = {title, url, description, rating}) => ({title, url, description, rating}
(myFunc)(this.state);

. . .

const myState = this.state
const myFunc = ({title, url, description, rating}) => {
  return {title, url, description, rating};
}
myFunc(myState);

. . .

const myState = this.state
const myFunc = props => {
  const { title, url, description, rating } = props;
  return {title, url, description, rating};
}
myFunc(myState);

. . .

this.state = {
  title: 'sup, world',
  url: 'www.michaelcruz.io',
  description: 'functional programming'
};
const myState = this.state;
const myFunc = props => {
  return props;
};
myFunc(myState);
```
